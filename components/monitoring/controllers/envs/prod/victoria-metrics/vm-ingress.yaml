vmsingle:
  route:
    enabled: true
    parentRefs:
      - name: private-gateway
        namespace: envoy-gateway-system
    hostnames:
      - vmks.infra.${EXTERNAL_DOMAIN}

grafana:
  route:
    main:
      enabled: true
      parentRefs:
        - name: private-gateway
          namespace: envoy-gateway-system
      hostnames:
        - grafana.infra.${EXTERNAL_DOMAIN}

---
apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: advertisement
spec:
  ipAddressPools:
    - default-pool
---
apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: default-pool
spec:
  addresses:
    - "${METALLB_IPV4_LB_RANGE}"
